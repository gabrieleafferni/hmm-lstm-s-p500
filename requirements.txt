# Dependencies for the hybrid HMM-LSTM S&P 500 forecasting pipeline.
# Lower bounds reflect the APIs actually used in the notebook; install with:
#     pip install -r requirements.txt
#
# For an exactly reproducible environment, freeze your working setup instead:
#     pip freeze > requirements.lock.txt

# Core scientific stack
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
statsmodels>=0.14

# Regime model, volatility benchmark, and neural network
hmmlearn>=0.3.0
arch>=6.0
tensorflow>=2.15

# Data acquisition (market data + FRED macro series)
yfinance>=0.2.40
fredapi>=0.5.1

# Plotting
matplotlib>=3.7
seaborn>=0.13

# Utilities and notebook runtime
joblib>=1.3
psutil>=5.9
ipython>=8.0
jupyter>=1.0
